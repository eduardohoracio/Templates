---
- hosts: all
  become: true
  vars:
     packages: [ 'clamav', 'clamav-daemon' ]
  tasks:
     - name: Install Packages
       apt: name={{ item }} state=latest
       with_items: "{{ packages }}"
